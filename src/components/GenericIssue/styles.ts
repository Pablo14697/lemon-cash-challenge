import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
    alignItems: 'center',
    paddingTop: '50%',
    backgroundColor: '#fafafa',
  },
  emptyLogo: { height: 100, width: 100 },
});

export default styles;
